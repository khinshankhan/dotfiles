# -*- mode: snippet -*-
# name: rfc
# key: rfc
# --
import React from "react"

const `(s-upper-camel-case (file-name-sans-extension (buffer-name)))` = ({$1}) => {
  return <>$2</>
}

export default `(s-upper-camel-case (file-name-sans-extension (buffer-name)))`;
