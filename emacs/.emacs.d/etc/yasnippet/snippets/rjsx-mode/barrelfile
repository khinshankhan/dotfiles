# -*- mode: snippet -*-
# name: barrelfile
# key: barrelfile
# --
/* eslint-disable import/prefer-default-export */
import `(file-name-nondirectory (directory-file-name (file-name-directory (buffer-file-name))))` from "./`(file-name-nondirectory (directory-file-name (file-name-directory (buffer-file-name))))`";

export default `(file-name-nondirectory (directory-file-name (file-name-directory (buffer-file-name))))`;